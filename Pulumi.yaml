name: pulumi_infra
description: A minimal Python Pulumi program to create a test repo for the resource importer CLI
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
